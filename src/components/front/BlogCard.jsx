import React from 'react';

import { Typography } from '@components/commons';

const BlogCard = () => {
  return (
    <div>
      {/* Image */}
      <img alt="" src="" />
      {/* Title */}
      <Typography variant="blogCardTitle">
        Building a stronger brand with no-code tools
      </Typography>
    </div>
  );
};

export default BlogCard;
