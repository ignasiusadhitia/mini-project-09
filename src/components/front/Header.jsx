import React from 'react';

import { Typography } from '@components/commons';

const Header = () => {
  return (
    <header>
      <Typography variant="headerText">HeaderText</Typography>
      {/* Illustration */}
      <img alt="" src="" />
    </header>
  );
};

export default Header;
